description: >-
  Object that holds links pertaining to the resource or resources currently
  being requested
type: object
properties:
  self:
    type: string
    format: url
    description: Link containing the URL used to reference the current document
    example: 'https://api.thingful.net/things/bj7ajg91'
