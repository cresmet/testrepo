name: perPage
in: query
description: >
  This parameter is used when paging through result sets to control how many
  results are returned within a single request.


  This parameter can be used both within search results and time series queries
  expressed at the channel level.
schema:
  type: integer
  minimum: 1
  maximum: 1000
