name: radius
in: query
description: >-
  Radius in metres defining the search area around the point specified by `long`
  and `lat`
schema:
  type: number
  format: float
  minimum: 10
  maximum: 10000
