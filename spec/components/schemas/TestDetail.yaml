allOf:
  - type: object
    properties:
      version:
        type: string
      exported_at:
        type: integer
      environments:
        $ref: '#/components/schemas/Environment'
      schedules:
        type: array
        items:
          $ref: '#/components/schemas/Schedule'
      steps:
        type: array
        items:
          type: object
      last_run:
        type: object
  - $ref: '#/components/schemas/Test'
